// Cấu hình cố định của bot (cấu hình bí mật / thay đổi được nằm trong .env)

// Discord ID của root (Mai Xuân Hiếu): người duy nhất dùng được các lệnh admin /
// test và nhận cảnh báo của bot
const ADMIN_DISCORD_ID = "747134485946171403";

// Tên của root trên sheet đăng ký cơm, dùng để kiểm tra kết nối sheet
const ADMIN_SHEET_NAME = "Mai Xuân Hiếu";

module.exports = {
    ADMIN_DISCORD_ID,
    ADMIN_SHEET_NAME,
};
